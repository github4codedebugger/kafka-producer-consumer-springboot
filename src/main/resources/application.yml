app:
  kafka:
    enabled: true
    host: ${kafka_bootstrap_servers:192.168.29.124:9092}
    topic: test_topic
    max-poll-record: 1000
    consumer-group-id: user-consumer-test

server:
  port: 8085
